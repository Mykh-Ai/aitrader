Ти працюєш у репозиторії AiTrader.

Роль:
Ти — research operations agent / deterministic replay operator.
Виконуй рутинний research цикл. Не вигадуй нових правил стратегії.

==================================================
РЕЖИМ ВИВОДУ
==================================================

Виконуй усі команди мовчки.
Не коментуй кожен крок у чаті.
Надай ТІЛЬКИ фінальний звіт у форматі A–F в кінці.

==================================================
ПЕРЕД ПОЧАТКОМ — ОБОВ'ЯЗКОВО ПРОЧИТАЙ
==================================================

1. `research/OPS.md` — операційний довідник:
   - SSH + venv, шляхи на сервері
   - outcome states таблиця
   - run_log.csv протокол (replace UNPROCESSED, не дублювати)

2. `research/run_log.csv` — журнал вже оброблених runs

3. `research/findings/` — зафіксовані research findings (не міняй)

==================================================
ЗАДАЧА ЦЬОГО ЦИКЛУ
==================================================

## Крок 1 — Запустити research_cycle.py на сервері

Один виклик замість ручного probe/replay/record:

```bash
ssh server "cd /opt/aitrader && source .venv/bin/activate && python3 research_cycle.py"
```

Скрипт автоматично:
- знаходить unprocessed runs (_processed.json відсутній)
- probe кожного (setups, shortlist, FormalizationEligible)
- replay через backtester якщо FE > 0 (заморожені параметри)
- читає promotion decisions
- пише _processed.json маркери
- запускає slice analysis
- виводить JSON на stdout

Для тестового запуску без replay:
```bash
ssh server "cd /opt/aitrader && source .venv/bin/activate && python3 research_cycle.py --dry-run"
```

Збережи повний JSON output — він потрібен для всіх наступних кроків.

Важливе правило для цього кроку:
- Не обробляй поточний календарний день, якщо UTC-day ще не завершений.
- Якщо cycle запускається сьогодні, current day вважається незавершеним trading/research day за замовчуванням.
- Не запускай analyzer/replay/slice на runs або feed поточного дня.
- Працюй тільки з повністю завершеними днями <= yesterday по UTC.
- Приклад: якщо сьогодні 2026-04-10, day 2026-04-10 не чіпати; останній допустимий день для routine cycle — 2026-04-09.

## Крок 2 — Валідація якості даних

Перевір секцію `diagnostics` в JSON output:

- `feed_coverage.missing_days` — чи є пропуски у feed?
- `analyzer_coverage` — скільки runs total/processed/unprocessed?
- `unprocessed_anomalies` — чи є runs з missing/empty артефактами?
- `data_quality_flags` — патерни (consecutive FE=0 тощо)

Якщо є anomalies — зафіксуй в handoff, не ігноруй.

## Крок 3 — Перевірити ключовий project signal

З JSON output перевір `results[].promotion_outcome`:
- Чи є хоч один != REJECT?
- Якщо ні — all-REJECT, research mode continues

## Крок 4 — Порівняти slice analysis з попереднім snapshot

Slice output є в JSON (`slice_output`).
Порівняти з попереднім snapshot:
- dir: `research/results/`
- naming: `reclaim_context_asymmetry_summary_<YYYY-MM-DD>.csv`
- baseline (без дати): `reclaim_context_asymmetry_summary.csv`
- знайти файл з найсвіжішою датою або baseline, якщо dated файлів ще немає

Якщо результати суттєво змінились (n, pos_rate, mean_ret дельта > noise) —
зафіксувати в новому файлі:
`research/results/reclaim_context_asymmetry_summary_<YYYY-MM-DD>.csv`

## Крок 5 — Оновити run_log.csv

З JSON output `results[]` оновити `research/run_log.csv`:
- Якщо рядок з `routine_status=UNPROCESSED` вже є → **replace**
- Якщо рядка немає → **append**
- Ніколи не дублювати рядки для одного `analyzer_run_id`

Формат рядка:
```
YYYY-MM-DD,<run_id>,<backtest_output_dir>,<FE>,<promotion_outcome>,<routine_status>,<notes>
```

## Крок 6 — Сформувати handoff для архітектора

Очисти `research/handoff/` (якщо є залишки) і запиши:

### cycle_meta.json
```json
{
  "cycle_date": "YYYY-MM-DD",
  "runs_processed": N,
  "runs_backtested": N,
  "runs_no_replay": N,
  "scope": "MM-DD → MM-DD",
  "agent_commit": "<git commit hash від кроку 7>"
}
```

### probe_summary.csv
З JSON `results[].probe`:
```
run_id,setups,shortlist_rows,formalization_eligible,replay_status,promotion_outcome,EventDensityClass,RangeExpansionClass,FlowStressClass,PhaseHeuristicLabel
```
Останні 4 колонки — per-run regime labels з `analyzer_day_regime_report.csv`.
Якщо артефакт відсутній для конкретного run — значення `NA`.

### promotion_details.csv
З JSON `results[].promotion_details` (тільки backtested runs):
```
run_id,derived_run,scope,promotion_decision,validation_status,robustness_status
```

### slice_comparison.csv
Рядки з попереднього snapshot + рядки з нового, з колонкою `snapshot_date`:
```
snapshot_date,direction,slice,n,pos_rate_pct,mean_ret_pct,median_ret_pct,mean_mfe_pct,mean_mae_pct
```

### slice_raw_output.txt
З JSON `slice_output`.

### previous_verdict.md
Копія останнього файлу з `research/verdicts/` (файл з найсвіжішою датою).
Якщо verdicts/ порожній — пропустити, написати в cycle_meta.json: `"previous_verdict": "none"`.

### diagnostics.json
З JSON `diagnostics` — повна секція діагностики як є.

**Handoff НЕ комітиться в git** — він тимчасовий.

## Крок 7 — Git commit

Закомітити зміни:
- оновлений `research/run_log.csv`
- новий results snapshot (якщо є)

Записати commit hash в `research/handoff/cycle_meta.json` (поле `agent_commit`).

==================================================
SSH CLEANUP — ОБОВ'ЯЗКОВО ПІСЛЯ ЗАВЕРШЕННЯ
==================================================

Sandboxed AI agents (Codex, Claude Code тощо) можуть додавати
службових користувачів (напр. CodexSandboxUsers) до ACL директорії
~/.ssh під час SSH операцій. Це ламає permissions для нативного
SSH після завершення сесії.

**Після всіх SSH операцій (останній крок перед фінальним звітом):**

```powershell
icacls C:\Users\User\.ssh /remove *S-1-5-21-3584294112-1179844679-616002924-1003
icacls C:\Users\User\.ssh\config /inheritance:r /grant:r "User:(R)" "SYSTEM:(R)" "Administrators:(R)"
```

Перевір що працює:
```powershell
ssh root@95.216.139.172 "echo ssh-ok"
```

Якщо ssh-ok — cleanup пройшов. Якщо ні — НЕ завершуй роботу,
а спробуй також:
```powershell
icacls C:\Users\User\.ssh\id_* /inheritance:r /grant:r "User:(R)"
```

**Це обов'язковий крок. Не пропускай.**

==================================================
ЧОГО НЕ МОЖНА РОБИТИ
==================================================

- Не міняти threshold / slice logic
- Не міняти параметри backtester
- Не міняти analyzer / backtester код
- Не міняти research_cycle.py
- Не пропонувати production changes поки немає REVIEW/PROMOTE
- Не змішувати `backtest_runs/` smoke-копії з основними runs

==================================================
ФОРМАТ ФІНАЛЬНОГО ЗВІТУ
==================================================

A. Preflight
- repo root, branch
- research_cycle.py вивід: runs found, scope

B. Per-run результати
Для кожного run з JSON:
- run_id
- setups / shortlist / FE
- replay status
- promotion outcome

C. Ключовий project signal
- перший REVIEW/PROMOTE? Або all-REJECT?

D. Slice analysis update
- що змінилось (або "без змін")

E. Діагностика якості
- feed coverage gaps
- anomalies
- data quality flags

F. Що записано
- _processed.json маркери (записані скриптом)
- рядки в run_log.csv
- handoff/ файли
- git commit hash

G. Наступний крок
- один вузький крок
