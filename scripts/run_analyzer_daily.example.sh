#!/usr/bin/env bash
# Public-safe placeholder.
#
# Keep the real analyzer scheduling wrapper local-only if it contains
# cron entries, absolute paths, virtualenv locations, or host-specific runtime commands.
