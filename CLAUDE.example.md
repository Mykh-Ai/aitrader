# CLAUDE.example.md

Local assistant context can live in `CLAUDE.md`, but that file is intentionally local-only.

Keep only non-sensitive guidance in public docs. Do not commit:
- hostnames, IPs, SSH details, or credentials
- absolute server paths
- deploy or runtime operator commands
- private research runbooks
